- if answer.persisted? && answer&.not_best_of?(question)
  p= answer.body

  - if current_user&.author_of?(answer)
    p= link_to 'Edit', '#', class: 'edit-answer-link btn btn-info btn-sm', data: { answer_id: answer.id }

    p= link_to 'Delete answer',
                answer_path(answer),
                method: :delete,
                remote: true,
                class: 'btn btn-danger btn-sm'

    p= link_to 'Mark as best',
                mark_as_best_answer_path(answer),
                method: :post, remote: true,
                class: 'btn btn-success btn-sm'

    = form_with model: answer, class: 'hidden', html: { id: "edit-answer-#{answer.id}" }, local: false do |f|
      = f.label :body, 'Your answer'
      = f.text_area :body
      = f.submit 'Save'
